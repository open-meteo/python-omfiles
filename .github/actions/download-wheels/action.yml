name: Download wheels
description: Download wheels artifact
inputs:
  name:
    required: true
  path:
    required: true
runs:
  using: "composite"
  steps:
    - name: Download wheel
      uses: actions/download-artifact@v8
      with:
        name: ${{ inputs.name }}
        path: ${{ inputs.path }}
